services:
  backend:
    build: .
    ports:
      - "8800:8000"
    env_file:
      - ../.env
    environment:
      DATABASE_URL: ${DATABASE_URL}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
